# Syslog Viewer Configuration
# Copy this file to /etc/syslog-viewer/syslog-viewer.env
# and set secure permissions: chmod 600 /etc/syslog-viewer/syslog-viewer.env

# REQUIRED: Password for web interface login
SYSLOG_PASSWORD=changeme

# REQUIRED: Secret key for session encryption (generate with: python3 -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=changeme

# Path to syslog file (default: /var/log/messages)
SYSLOG_FILE=/var/log/messages

# Number of log lines to load on startup (default: 2000)
INITIAL_LINES=2000

# SSL certificate paths (relative to WorkingDirectory or absolute)
SSL_CERT=/opt/syslog-viewer/cert.pem
SSL_KEY=/opt/syslog-viewer/key.pem

# Port to listen on (default: 443)
PORT=443
